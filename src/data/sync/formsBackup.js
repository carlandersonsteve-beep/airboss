export async function backupToForms(payload) {
  // Placeholder for the existing Google Forms backup behavior.
  // Keep no-cors form backup logic here instead of inside UI components.
  return {
    ok: true,
    payload,
    mode: 'placeholder',
  };
}
