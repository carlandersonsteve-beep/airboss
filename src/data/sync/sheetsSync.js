export async function syncToSheets(action, payload) {
  // Placeholder for the existing Google Apps Script integration.
  // Keep all sheet sync logic here instead of inside UI components.
  return {
    ok: true,
    action,
    payload,
    mode: 'placeholder',
  };
}
